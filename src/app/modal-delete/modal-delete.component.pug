ng-template(#content let-c="close" let-d="dismiss")
    .modal-header
        h4 Delete Comment
        button(type="button" class="close" aria-label="Close" '(click)'="d('Cross click')") X
    .modal-body
        .text-center
            i(class="fa fa-exclamation-circle")
        h5.text-center Are you sure you want to delete this record?
    .modal-footer
        button( class="btn btn-primary" '(click)'='answer(true)' ) Yes
        button( class="btn btn-danger"  '(click)'='answer(false)' ) No

